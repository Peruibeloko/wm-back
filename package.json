{
  "name": "wobblemakers-server",
  "version": "1.0.0",
  "description": "backend para CRUD de membros do selo",
  "main": "index.js",
  "scripts": {
    "start": "nodemon app.js --exec babel-node --presets env",
    "build": "del dist && mkdir dist && babel . -s -D -d dist --presets env --ignore node_modules",
    "lint": "prettier --write \"./**/*.{js,json}\" && eslint --fix --ignore-path .gitignore ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^7.5.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0",
    "nodemon": "^2.0.2",
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.9.7"
  }
}
